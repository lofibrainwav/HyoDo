# afo_core dependency SSOT is poetry.lock (not this file).
#
# Why this file is not a pin list:
# GitHub Dependabot was double-counting the same packages from poetry.lock AND
# requirements.txt (e.g. 60 + 60 = 120 open alerts). Security updates must be
# applied via poetry.lock only.
#
# Install extended afo_core stack:
#   cd afo_core && poetry install --all-extras
#
# Optional frozen export for offline/docker (do NOT commit the export):
#   poetry export -f requirements.txt --all-extras -o /tmp/afo-core-req.txt
#
# Public HyoDo package does not use this file:
#   pip install -e ".[dev]"   # repo root
#
# Placeholder requirement so some tooling that expects a file still works:
setuptools>=78.1.1
