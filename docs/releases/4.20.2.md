# HyoDo 4.20.2 Release Notes

## Intent

Publish the #441 lens-semantics correctness fix without mutating the
immutable 4.20.1 artifacts. Reconciliation must not turn tool names, metadata
tags, or an output digest alone into virtue-lens evidence. The legacy viewer may
retain compatibility placement, while the canonical reconciliation remains
evidence-bounded.

## Release chain receipt

Measured 2026-09-21T04:34:43Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.20.2` -> `8465095`, signature verified).
- OBSERVED — GitHub Release created (`v4.20.2` release exists).
- OBSERVED — Release evidence workflow (run `35561294881`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-21T04:32:19Z, not a draft).
- OBSERVED — PyPI publish (run `35561376085`).
- OBSERVED — PyPI provenance verified (run `35561376085`).
- OBSERVED — Install smoke (`4.20.2` on PyPI, wheel sha256 `26945c9fe48ee6bf`).
