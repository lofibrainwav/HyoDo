# HyoDo 4.20.1 Release Notes

## Intent

Reconcile post-4.20.0 evidence and public-surface changes while preserving the
boundary between host observations, derived projections, and authority. The
candidate improves Codex result observation and graph projection fidelity; it
does not infer terminal outcomes that the producer does not expose.

## Release chain receipt

Measured 2026-09-21T04:17:38Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.20.1` -> `be07640`, signature verified).
- OBSERVED — GitHub Release created (`v4.20.1` release exists).
- OBSERVED — Release evidence workflow (run `35557232731`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-21T03:22:21Z, not a draft).
- OBSERVED — PyPI publish (run `35557297728`).
- OBSERVED — PyPI provenance verified (run `35557297728`).
- OBSERVED — Install smoke (`4.20.1` on PyPI, wheel sha256 `edfe06a1cfa8f4f7`).
