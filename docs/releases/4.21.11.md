# HyoDo 4.21.11 Release Notes

## Intent

Publish the policy-configuration fail-closed fix merged after the immutable
4.21.10 tag. In 4.21.10 and earlier, `load_policy_config()` ignored unknown
`policy.toml` keys. A typo such as `allowed_toolz = ["search"]` could remove
the intended allowlist and let an unlisted tool fall through to `ALLOW`.
#506 rejects unknown root keys and unknown keys inside `[web]`, `[trust]`,
and `[ephemeral]` with `PolicyConfigError`. The existing invalid-policy path
then reports `policy_invalid` / `UNOBSERVED` with exit code 2. Valid
`hyodo.policy/v1` files keep identical semantics. This release adds no new
HyoDo Core capability.

## Verification measured before release

- Source commit: #506 ->
  `9e357c5ccb4f3663bae1592a7935c2963f3e6bd1`.
- Hostile pre-fix replay: `allowed_toolz = ["search"]` parsed with
  `allowed_tools=None` and an unlisted tool evaluated `ALLOW`.
- Post-fix real CLI replay: the same malformed policy reports
  `policy_invalid` / `UNOBSERVED`, exit code 2, never `ALLOW`.
- Focused policy/CLI suite: 182 passed. Full `scripts/verify-public.sh`:
  2,141 passed.
- Fresh main CI at `9e357c5`: HyoDo CI `36224531483`, Smoke Test
  `36224531444`, Security verification `36224531447`, CodeQL SAST
  `36224531476`, and OpenSSF Scorecard `36224531524` all completed
  successfully; `Goodness - Serial Full Suite (main)` succeeded.
- Immutable `v4.21.10` targets
  `b3d8da5b32aefcb553da3414c69177fbad30d347`; #506 merged afterwards and is
  not present in 4.21.10.

## Release chain receipt

Measured 2026-09-26T14:33:32Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.21.11` -> `230bf26`, signature verified by GitHub).
- OBSERVED — GitHub Release created (`v4.21.11` release exists).
- OBSERVED — Release evidence workflow (run `36227376912`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-26T07:39:19Z, not a draft).
- OBSERVED — PyPI publish (run `36227437446`).
- OBSERVED — PyPI provenance verified (run `36227437446`).
- OBSERVED — Install smoke (`4.21.11` on PyPI, wheel sha256 `c0c47a2c8fa17e01`).
