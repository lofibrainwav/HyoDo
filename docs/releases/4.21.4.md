# HyoDo 4.21.4 Release Notes

## Intent

Close the installed-wheel provenance false green found in 4.21.3. HyoDo now
compares the measuring package's actual `hyodo/` source content with a HyoDo
target instead of borrowing identity from an enclosing Git repository. A
different target source stays `MISMATCH` / `effective=UNOBSERVED`; identical
content remains observable even when the wheel lives outside a Git checkout.
This patch also includes the zero-detection `hyodo init` documentation
correction. No execution-authority or policy semantics change.

## Release chain receipt

Measured 2026-09-23T14:02:35Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.21.4` -> `e4da80c`, signature verified).
- OBSERVED — GitHub Release created (`v4.21.4` release exists).
- OBSERVED — Release evidence workflow (run `35869925959`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-23T13:51:59Z, not a draft).
- OBSERVED — PyPI publish (run `35870073157`).
- OBSERVED — PyPI provenance verified (run `35870073157`).
- OBSERVED — Install smoke (`4.21.4` on PyPI, wheel sha256 `7abfc0219e51804f`).
