# HyoDo 4.19.5 Release Notes

## Intent

4.19.5 is a public artifact convergence patch. It republishes the corrected
sdist scope from canonical `main` under a new immutable version; the existing
4.19.4 filename is not overwritten.

## Contract

- The wheel and sdist are built from one exact tagged source SHA.
- The sdist contains the public package, release documents, and runtime
  identity schemas, but no checkout tests or `afo_core` paths.
- The PyPI fresh-download hashes and byte size must equal the release-build
  receipt exactly.
- PyPI publication uses the repository's OIDC Trusted Publishing workflow and
  requires non-null provenance.

## Evidence status

The release chain below records the tag, GitHub Release, release CI, PyPI
fresh download, content scope, neutral install, and provenance readback for
4.19.5.

## Artifact receipt

- Source SHA: `e0cb9dc2aabc871cb573b229c8b333fe47f86804`.
- Wheel: `290852` bytes,
  SHA-256 `5430a88ab262593ca269ae315aa7238fbbae05fceb956995631dff3f6cc61d56`.
- Sdist: `290432` bytes, 70 entries,
  SHA-256 `be22813b7dc13287d4cb9d40d8b47a168e3b4e85087d2a4622596971dd944287`.
- Fresh PyPI downloads match both local build artifacts byte-for-byte.
- The sdist contains the runtime identity schema and pin, and excludes
  `afo_core` and checkout tests.
- Wheel and sdist PEP 740 provenance endpoints returned HTTP 200.

## Release chain receipt

Measured 2026-09-13T03:16:55Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.19.5` -> `e0cb9dc`, signature verified).
- OBSERVED — GitHub Release created (`v4.19.5` release exists).
- OBSERVED — Release evidence workflow (run `34734897799`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-13T03:13:47Z, not a draft).
- OBSERVED — PyPI publish (run `34734964117`).
- OBSERVED — PyPI provenance verified (run `34734964117`).
- OBSERVED — Install smoke (`4.19.5` on PyPI, wheel sha256 `5430a88ab262593c`).
