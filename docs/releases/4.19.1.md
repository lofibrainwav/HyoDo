# HyoDo 4.19.1 Release Notes

## Intent

This patch release makes Cursor and Codex native tool-hook payloads available
to HyoDo's canonical observation spine. It adds host adapters, preserves
privacy-minimized argument/output/path digests, and records platform contract,
adapter verification, and live-canary status as separate provenance axes.

The release does not claim that a fixture is a live host callback. Native
Cursor/Codex response serialization is implemented and fixture-verified, but
live host callback observation remains `UNOBSERVED`.

## Release chain receipt

Measured 2026-09-11T02:39:17Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.19.1` -> `a0a5dba`, signature verified).
- OBSERVED — GitHub Release created (`v4.19.1` release exists).
- OBSERVED — Release evidence workflow (run `34542339351`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-10T23:30:32Z, not a draft).
- OBSERVED — PyPI publish (run `34542461485`).
- OBSERVED — PyPI provenance verified (run `34542461485`).
- OBSERVED — Install smoke (`4.19.1` on PyPI, wheel sha256 `c4a3cb7c68246ddf`).
