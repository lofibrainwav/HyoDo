# HyoDo 4.21.10 Release Notes

## Intent

Publish the native hook root fix already merged on main. In 4.21.9, an
explicit `--root` on `hyodo event record --hook` / `hyodo policy check --hook`
was replaced by the host payload `cwd`, so a hook fired from a worktree wrote
that worktree's `.hyodo` ledger and read its policy and trust state. #501
makes an explicit `--root` the storage and policy root for both commands and
all native adapters; without `--root`, the payload `cwd` remains the fallback.
`hyodo connect claude-code` stops writing `--root .` into PreToolUse. This
release commit adds no new HyoDo Core capability.

## Verification measured before release

- Source commit: #501 -> `68f58cade006251d89a8ed0f9406bbc0cccdc8e7`; main CI
  green including `Goodness - Serial Full Suite (main)` (run `36220967905`).
- Source base: main `7d90db83421dbc1993ded783718f33a947faaa22` (#504); main CI
  green including `Goodness - Serial Full Suite (main)` (run `36223217600`).
- #503 and #504 are release tooling only (`scripts/release/pipeline.py`, not
  in the published package): CI checks are judged against per-event
  expected-N/A allowlists. On a PR only the main-only serial suite may be
  skipped; on main only the PR-only dependency review may be skipped, and the
  serial suite must succeed.
- Regression `tests/test_hook_explicit_root.py`: root=A / payload cwd=B keeps
  ledger, step count, and policy on A and creates no `B/.hyodo`; the no-root
  path still follows payload cwd.
- Live Claude Code hook-process-cwd vs payload-cwd divergence for settings
  still carrying `--root .` is `UNOBSERVED`.

## Release chain receipt

Measured partial state on 2026-09-26 UTC. The immutable release is not rewritten
to include later main changes.

- OBSERVED — Signed, GitHub-verified annotated tag `v4.21.10` targets
  `b3d8da5b32aefcb553da3414c69177fbad30d347`.
- OBSERVED — GitHub Release exists and is published.
- OBSERVED — Release evidence workflow `36224167136` completed successfully.
- OBSERVED — `sbom.cyclonedx.json` is attached.
- OBSERVED — `sbom.cyclonedx.json.sha256` is attached.
- OBSERVED — GitHub Release publication completed.
- UNOBSERVED — PyPI publication is not complete; publish workflow
  `36224231839` is waiting at the `pypi` environment approval boundary.
- UNOBSERVED — PyPI provenance verification awaits publication.
- UNOBSERVED — Install smoke awaits publication.
