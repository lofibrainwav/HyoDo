# HyoDo 4.21.8 Release Notes

## Intent

Publish the evidence/judgment boundary already merged on main. #482 preserves
gate-to-virtue attribution in `hyodo.dashboard-evidence/v2`; #483 adds
`hyodo.lens-evidence/v1` with `authority: UNOBSERVED`, keeps proxy coverage
distinct from virtue coverage, and freezes legacy score derivation as
compatibility only. This release commit adds no new HyoDo Core capability.

## Verification measured before release

- Exact main base `872ecdca1ffa4c51682fd6b6ada586851cb2ec19` CI readback:
  required checks are success; one conditional dependency-review job is
  skipped.
- Release-chain steps were `UNOBSERVED` until measured after publication; the
  measured receipt follows.

## Release chain receipt

Measured 2026-09-24T21:50:14Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.21.8` -> `aa270e0`, signature verified).
- OBSERVED — GitHub Release created (`v4.21.8` release exists).
- OBSERVED — Release evidence workflow (run `36063280175`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-24T21:44:53Z, not a draft).
- OBSERVED — PyPI publish (run `36063407919`).
- OBSERVED — PyPI provenance verified (run `36063407919`).
- OBSERVED — Install smoke (`4.21.8` on PyPI, wheel sha256 `0dab4e34030abf53`).
