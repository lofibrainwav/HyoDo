# HyoDo 4.21.8 Release Notes

## Intent

Publish the evidence/judgment boundary already merged on main. #482 preserves
gate-to-virtue attribution in `hyodo.dashboard-evidence/v2`; #483 adds
`hyodo.lens-evidence/v1` with `authority: UNOBSERVED`, keeps proxy coverage
distinct from virtue coverage, and freezes legacy score derivation as
compatibility only. This release commit adds no new HyoDo Core capability.

## Verification measured before release

- Exact main base `872ecdca1ffa4c51682fd6b6ada586851cb2ec19` CI readback:
  required checks are success; one conditional dependency-review job is
  skipped.
- Release-chain steps below are not measured yet. They remain `UNOBSERVED`
  until the 4.21.8 tag, GitHub Release, SBOM, PyPI publication, provenance,
  and install smoke are each read back.
