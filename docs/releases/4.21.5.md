# HyoDo 4.21.5 Release Notes

## Intent

Close the MCP reader cutover blind spot found during the 4.21.4 runtime
promotion: a `hyodo mcp stdio` reader started before the promotion kept
answering for the retired slot, and nothing outside the process could see it.
Readers now register themselves, `hyodo mcp census` reports whether every live
reader converged on the promoted slot, and a stale reader refuses measurement
tools instead of answering. HyoDo observes only; the host keeps promotion
authority. This patch also carries the publication-pipeline interpreter fix
from #473.

## Release chain receipt

Measured 2026-09-23T18:02:02Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.21.5` -> `1743690`, signature verified).
- OBSERVED — GitHub Release created (`v4.21.5` release exists).
- OBSERVED — Release evidence workflow (run `35895473223`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-23T17:26:29Z, not a draft).
- OBSERVED — PyPI publish (run `35895615574`).
- OBSERVED — PyPI provenance verified (run `35895615574`).
- OBSERVED — Install smoke (`4.21.5` on PyPI, wheel sha256 `932c3db0b97538c1`).
