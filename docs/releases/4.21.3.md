# HyoDo 4.21.3 Release Notes

## Intent

Publish the 4.21.2 content (the verification-plumbing honesty patch) together
with the publication-order fix from #463 through the corrected pipeline. The
pipeline itself owns publication as a strictly linear state chain and refuses
wrong order, so a GitHub Release cannot be published before its SBOM evidence
again. 4.21.2 remains an incomplete immutable historical release and is not
republished or retagged.

## Release chain receipt

Not measured yet. Run `python -m scripts.release.verify_release_chain <version>`
to replace this section with a measured receipt. `UNOBSERVED` means nobody has
looked, which is not the same as a step that failed.

- UNOBSERVED — Signed verified tag.
- UNOBSERVED — GitHub Release created.
- UNOBSERVED — Release evidence workflow.
- UNOBSERVED — SBOM asset attached.
- UNOBSERVED — SHA-256 receipt attached.
- UNOBSERVED — Release published.
- UNOBSERVED — PyPI publish.
- UNOBSERVED — PyPI provenance verified.
- UNOBSERVED — Install smoke.
