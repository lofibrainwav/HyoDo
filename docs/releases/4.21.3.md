# HyoDo 4.21.3 Release Notes

## Intent

Publish the 4.21.2 content (the verification-plumbing honesty patch) together
with the publication-order fix from #463 through the corrected pipeline. The
pipeline itself owns publication as a strictly linear state chain and refuses
wrong order, so a GitHub Release cannot be published before its SBOM evidence
again. 4.21.2 remains an incomplete immutable historical release and is not
republished or retagged.

## Release chain receipt

Measured 2026-09-22T23:29:53Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.21.3` -> `9004ae4`, signature verified).
- OBSERVED — GitHub Release created (`v4.21.3` release exists).
- OBSERVED — Release evidence workflow (run `35796483911`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-22T23:16:06Z, not a draft).
- OBSERVED — PyPI publish (run `35796591943`).
- OBSERVED — PyPI provenance verified (run `35796591943`).
- OBSERVED — Install smoke (`4.21.3` on PyPI, wheel sha256 `4933774ed95bd4a7`).
