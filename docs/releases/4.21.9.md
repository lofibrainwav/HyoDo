# HyoDo 4.21.9 Release Notes

## Intent

Publish the provenance fix already merged on main so HyoDo no longer reports
unknown as different. In 4.21.8, an unreadable source subtree could be
classified `SELF_OTHER_CHECKOUT` / `MISMATCH` because `Path.rglob` could hide a
directory-scan `OSError`. #488 makes that case `SOURCE_UNOBSERVED` /
`UNOBSERVED` with `green_allowed=false`. #487 is test-only deterministic
fixture hardening; it is verification evidence, not product capability. This
release commit adds no new HyoDo Core capability.

## Verification measured before release

- Source base: main `1b3a0c1e417e1d136154b45308dc96c9fcb94790` (#488).
- Release-chain steps stay `UNOBSERVED` until they are measured after
  publication.

## Release chain receipt

Not measured yet. Run `python -m scripts.release.verify_release_chain <version>`
to replace this section with a measured receipt. `UNOBSERVED` means nobody has
looked, which is not the same as a step that failed.

- UNOBSERVED — Signed verified tag.
- UNOBSERVED — GitHub Release created.
- UNOBSERVED — Release evidence workflow.
- UNOBSERVED — SBOM asset attached.
- UNOBSERVED — SHA-256 receipt attached.
- UNOBSERVED — Release published.
- UNOBSERVED — PyPI publish.
- UNOBSERVED — PyPI provenance verified.
- UNOBSERVED — Install smoke.
