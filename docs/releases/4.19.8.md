# HyoDo 4.19.8 Release Notes

## Intent

HyoDo 4.19.8 converges the public package, installed CLI, and runtime identity
contract on one version. It also adds a read-only runtime-slot GC classifier
that protects active, pinned, dirty-unique, process-held, and registered
worktree surfaces without stopping services or removing worktrees.

Publication and host-owned runtime promotion remain separate from this source
release preparation.

## Release chain receipt

Measured 2026-09-17T08:31:32Z by `scripts/release/verify_release_chain.py`.

- OBSERVED — Signed verified tag (`v4.19.8` -> `ad4798e`, signature verified).
- OBSERVED — GitHub Release created (`v4.19.8` release exists).
- OBSERVED — Release evidence workflow (run `35199753151`).
- OBSERVED — SBOM asset attached (`sbom.cyclonedx.json`).
- OBSERVED — SHA-256 receipt attached (`sbom.cyclonedx.json.sha256`).
- OBSERVED — Release published (published 2026-09-17T08:28:35Z, not a draft).
- OBSERVED — PyPI publish (run `35199864613`).
- OBSERVED — PyPI provenance verified (run `35199864613`).
- OBSERVED — Install smoke (`4.19.8` on PyPI, wheel sha256 `d1f5e6d6e3d33c8a`).
